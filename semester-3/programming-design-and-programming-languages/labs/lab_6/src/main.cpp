#include <fun.h>

int main() {
    // SchoolKid sch;
    // Student std;
    // SchoolKid schK;
    // GradeStudent grS;
    // int size = 4;
    // Pupil** some  = new Pupil* [size];
    // some[0] = &sch;
    // some[1] = &std;
    // some[2] = &schK;
    // some[3] = &grS;
    // prntPupils(some,size);
    mainMenu();
    return 0;
}